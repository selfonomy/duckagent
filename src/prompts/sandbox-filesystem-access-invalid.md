Tool error: invalid_request
Capability: request_filesystem_access
Field: {{field}}
Reason: {{reason}}
Path: {{path}}
{{parent_line}}
Fix the request and call `request_filesystem_access` again only if the user still needs this filesystem access and provides or confirms a concrete file or directory path.
